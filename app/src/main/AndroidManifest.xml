<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pwr.inteligentbuilding">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InteligentBuilding">
        <activity
            android:name=".activity.OpcuaActivity"
            android:exported="true" />
        <activity
            android:name=".activity.SettingsActivity"
            android:exported="true" />
        <activity android:name=".activity.MainActivity" />
        <activity
            android:name=".activity.RoomActivity"
            android:exported="true" />
        <activity
            android:name=".activity.LightActivity"
            android:exported="true" />
        <activity
            android:name=".activity.FloorActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>